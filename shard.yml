name: rcm
version: 0.5.2

dependencies:
  redis-cluster:
    github: maiha/redis-cluster.cr
    version: 0.6.4
  kemal:
    # TODO: change to official repo after "without_openssl" is released
    github: maiha/kemal
    branch: without_openssl
  crt:
    github: maiha/crt.cr
    version: 0.1.2

authors:
  - maiha <maiha@wota.jp>

license: MIT
